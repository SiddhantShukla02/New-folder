<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= doctor.name %> | Doctor Details</title>

  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f8f9fa;
      margin: 0;
      padding: 0;
    }

    header {
      background: #0052cc;
      color: white;
      padding: 20px;
      text-align: center;
    }

    main {
      max-width: 800px;
      margin: 30px auto;
      background: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    h1 {
      color: #333;
      margin-bottom: 10px;
    }

    p {
      margin: 6px 0;
      color: #555;
    }

    .info-section {
      margin-bottom: 25px;
    }

    h2 {
      color: #0052cc;
      margin-bottom: 10px;
      border-bottom: 2px solid #0052cc;
      display: inline-block;
      padding-bottom: 5px;
    }

    .hospital-card {
      background: #f0f4ff;
      border-radius: 8px;
      padding: 15px;
      border: 1px solid #cdd9ff;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
      margin-top: 15px;
    }

    .hospital-card a {
      color: #0052cc;
      font-weight: 600;
      text-decoration: none;
    }

    .hospital-card a:hover {
      text-decoration: underline;
    }

    footer {
      text-align: center;
      padding: 15px;
      background: #f0f0f0;
      color: #777;
      margin-top: 30px;
    }

    a.back-btn {
      display: inline-block;
      margin-top: 10px;
      color: #0052cc;
      text-decoration: none;
      font-weight: 600;
    }

    a.back-btn:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <header>
    <h1><%= doctor.name %></h1>
  </header>

  <main>
    <div class="info-section">
      <h2>Doctor Information</h2>
      <p><strong>Specialization:</strong> <%= doctor.specialization %></p>
      <p><strong>Experience:</strong> <%= doctor.experience %> years</p>
      <p><strong>Contact:</strong> <%= doctor.contact || 'N/A' %></p>
      <% if (doctor.email) { %>
        <p><strong>Email:</strong> <%= doctor.email %></p>
      <% } %>
      <% if (doctor.qualifications && doctor.qualifications.length > 0) { %>
        <p><strong>Qualifications:</strong> <%= doctor.qualifications.join(", ") %></p>
      <% } %>
    </div>

    <div class="info-section">
      <h2>Associated Hospital</h2>
      <% if (doctor.hospital) { %>
        <div class="hospital-card">
          <h3><a href="/hospitals/<%= doctor.hospital._id %>"><%= doctor.hospital.name %></a></h3>
          <p><strong>Address:</strong> <%= doctor.hospital.address %></p>
          <p><strong>City:</strong> <%= doctor.hospital.city %></p>
        </div>
      <% } else { %>
        <p>This doctor is not yet assigned to any hospital.</p>
      <% } %>
    </div>

    <a href="/doctors" class="back-btn">← Back to Doctors List</a>
  </main>

  <footer>
    <p>© 2025 Hospital Management Portal</p>
  </footer>

</body>
</html>
